language: go
go:
    - "1.11"
    - "1.12"

install:
  - git config remote.origin.fetch +refs/heads/*:refs/remotes/origin/*
  - git fetch --unshallow --tags

script: make ci

cache:
    directories:
        # - $GOPATH/github.com/jeffrom/logd/vendor
        - $GOPATH/bin
        - $GOPATH/pkg
